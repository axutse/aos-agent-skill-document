interface:
  display_name: "AOS PDF Processing"
  short_description: "Inspect, render, clean, and verify fixed-layout PDFs"
  default_prompt: "Use $aos-process-pdf to audit this PDF structurally and visually before making any changes."
